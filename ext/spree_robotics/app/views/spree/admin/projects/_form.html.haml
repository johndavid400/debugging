= form_for ([:admin, @project]) do |f|
  - if @project.errors.any?
    #error_explanation
      %h2
        = pluralize(@project.errors.count, "error")
        prohibited this tutorial from being saved:
      %ul
        - @project.errors.full_messages.each do |msg|
          %li= msg

  %fieldset
    %legend
      Project
    .clearfix
      = f.label :name
      .input= f.text_field :name
    .clearfix
      = f.label :description
      .input= f.cktext_area :description
    .clearfix
      = f.label :category
      .input= f.text_field :category
    .clearfix
      = f.label :featured
      .input= f.check_box :featured
    .clearfix
      = f.label :published
      .input= f.check_box :published
    .clearfix
      = f.label :remove_image
      .input= f.check_box :remove_image
    - if @project.image
      .clearfix
        .centered_image= image_tag(@project.image.url(:slider), :class => "imgshadow")
    .clearfix
      = f.label :image
      .input= f.file_field :image

  .actions
    = f.submit :submit, :class => 'btn primary'
    = link_to 'Back', tutorials_path, :class => 'btn'
